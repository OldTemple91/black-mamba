# 2026-03-16 Daily Log

## 오늘 한 일

- `mixed-winning` 샘플을 `4건 -> 7건`으로 확대
- `TIME_PRIORITY` / `RELIABILITY` 기준으로 7건 전체 재검증
- `no-mixed` 샘플 4건 확보 및 `reasonCode` 집계 확인
- `SAME_PICKUP_DROPOFF`가 반복적으로 발생하는 케이스를 줄이기 위해 `mobility_segment` 캐시 및 세그먼트 단위 가용성 조회 추가
- 실제 경로 생성 단계에서 동일 정류소 대여/반납 조합을 더 일찍 제외하도록 구조 정리

## 핵심 결과

### mixed-winning

- 샘플 수: `7`
- `TIME_PRIORITY`: mixed `7/7` 추천
- `RELIABILITY`: `TRANSIT_ONLY 7/7`
- 평균 시간 차이: `-3.857분`
- 평균 비용 차이: `-271원`

### no-mixed

- 샘플 수: `4`
- mixed alternative: `0`
- 수집된 진단 코드:
  - `NO_CANDIDATE_HUB`
  - `NO_PICKUP`
  - `NO_DROPOFF`
- latest rerun summary:
  - `NO_CANDIDATE_HUB: 6`
  - `NO_PICKUP: 1`
  - `NO_DROPOFF: 1`

## 코드 변경

- `MobilityAvailabilityPort`에 `findSegmentMobility(...)` 추가
- `MobilityAvailabilityAdapter`에 `mobility_segment` 캐시 추가
- `SpecificMobilityStrategy`, `OptimalSearchStrategy`가 세그먼트 단위 조회를 사용하도록 변경
- 관련 테스트 stub 정리 및 전체 테스트 통과 확인

## 검증

```bash
./gradlew :application:test :api:test :domain:test :infra:test --no-daemon
```

- 결과: `BUILD SUCCESSFUL`

### runtime cache 확인

- 최신 코드 서버를 `8082`로 별도 부팅해 actuator metric 확인
- `mobility_segment` 캐시 태그 노출 확인
- `SAME_PICKUP_DROPOFF` early pruning 적용 후 `no-mixed` 재실행에서 해당 reasonCode가 사라짐
- warm second-pass 관찰:
  - `tmap_pedestrian_route missDelta = 0`
  - `mobility_availability` / `mobility_segment`는 허브·세그먼트 조합 다양성 때문에 miss가 일부 유지

## 다음 우선순위

1. `캠퍼스 / 박물관 / 업무지구 내부` 축에서 mixed winner가 안 나오는 이유를 바탕으로 허브 선택 규칙 보강
2. `NO_CANDIDATE_HUB`가 많은 샘플을 기준으로 퍼스트/라스트마일 허브 생성 규칙 조정
3. `mobility_availability` / `mobility_segment` miss를 더 줄일 수 있는 dedup 키 설계 검토

# 2026-03-18 Daily Log

## 오늘 한 일

- `10분+`, 가능하면 `15분+` 차이가 나는 개인 이동수단 강한 케이스 탐색
- `SPECIFIC + PERSONAL + TIME_PRIORITY`를 `대중교통 only`와 직접 비교
- 개인 이동수단 strong case를 별도 샘플 세트로 분리

## 핵심 결과

- `jamwon -> 반포한강공원`
  - `36분 -> 16분`
  - `20분` 단축
  - 비용 동일
- `banpo -> 잠원한강공원`
  - `32분 -> 20분`
  - `12분` 단축
  - 비용 동일
- `mangwon -> 월드컵공원`
  - `31분 -> 17분`
  - `14분` 단축
  - 비용 `+50원`
- `mapo -> 난지캠핑장`
  - `35분 -> 25분`
  - `10분` 단축
  - 비용 동일

## 현재 판단

- 기본 `OPTIMAL`에서 개인 이동수단을 제외한 판단은 여전히 맞다.
- 다만 사용자 보유 이동수단을 전제로 한 별도 실험 축에서는, 공유/공공 모빌리티보다 훨씬 큰 시간 단축 가능성이 확인된다.
- 따라서 이 프로젝트에서는
  - `현실적 MaaS 검증`
  - `개인 이동수단 상한선 실험`
  을 분리해 설명하는 것이 가장 자연스럽다.
- 허브 구조 측면에서는 `HubSearchPort`를 도입해, 후보 검색 책임과 허브 선택 책임을 분리하는 첫 단계를 시작했다.

# 2026-03-20 Daily Log

## 오늘 한 일

- 전날 적용한 `relaxed fallback` 허브 규칙을 최신 서버(`8081`) 기준으로 재검증
- `no-mixed` / `mixed-winning` 실험 세트를 다시 실행해 실제 영향 확인
- README / 실험 문서 / current focus를 최신 수치로 갱신

## 재검증 결과

- `no-mixed` 4건
  - `recommendedTransitOnlyCount: 4`
  - `samplesWithMixedAlternative: 1 -> 2`
  - `recommendedGenerationReasonCounts`
    - `NO_PICKUP: 4`
- `mixed-winning` 7건
  - `TIME_PRIORITY`에서 mixed `7/7` 유지
  - 평균 `3.857분` 단축
  - 평균 비용 `-57원`

## 해석

- `relaxed fallback`은 `NO_CANDIDATE_HUB` 중심 병목을 줄이는 방향으로 작동했다.
- mixed 대안은 더 생겼지만, 추천은 여전히 `TRANSIT_ONLY`로 유지돼 `no-mixed` 세트의 성격은 크게 흔들리지 않았다.
- 동시에 `mixed-winning` 세트는 그대로 유지돼, fallback 완화가 winner 품질을 해치지 않았다는 점을 확인했다.
- 현재 남은 주 병목은 허브 부재보다 `NO_PICKUP` 쪽이다.

## 다음 단계

- `NO_PICKUP`가 반복되는 샘플에서 허브 주변 최근접 대여소 근접도를 후보 생성 단계에 더 직접 반영
- 허브가 살아난 `no-mixed` 2건을 비교해 어떤 fallback 허브가 mixed 대안을 만들었는지 확인
- 필요하면 fallback 허브의 selection metadata를 더 자세히 노출

# 2026-03-12 Daily Log

## 오늘 한 일

### 1. RouteEvaluation 도메인화

- `RouteEvaluation` 도메인 객체 추가
- 시간/환승/비용/도보/접근도보/신뢰도 세부 점수를 모두 저장
- 총 도보, 환승 수, 최대 접근 도보, 가용성/반납/배터리 리스크도 함께 저장
- `RouteEvaluator`가 점수 숫자만 넣는 대신 평가 객체까지 `Route`에 주입하도록 변경

관련 파일:

- `domain/route/RouteEvaluation`
- `application/route/RouteScoreCalculator`
- `application/route/RouteEvaluator`
- `domain/route/Route`

### 2. Hub 정보 API 노출

- `RouteHub` 도메인 추가
- 실제 route leg를 기준으로 `BUS_STOP`, `SUBWAY_STATION`, `BIKE_STATION`, `MOBILITY_TRANSFER_POINT` 허브를 추출
- `RouteEvaluation.hubs`로 API 응답에 허브 타입/역할 정보 포함
- 전략 계층에서 선택한 candidate hub를 `selected-candidate` source로 보존
- `selectionPhase`, `preferredMobility` metadata를 응답/디버그에 포함

관련 파일:

- `domain/route/RouteHub`
- `application/route/RouteHubExtractor`
- `api/test/.../RouteControllerTest`

### 3. 프론트 허브/평가 표시

- 경로 카드에 `경로 허브` 섹션 추가
- 디버그 패널에 평가 점수와 허브 목록 노출
- 비용 상세와 허브 정보가 같은 응답 모델을 재사용하도록 정리
- 선택 허브는 `후보(출발)` / `후보(도착)`으로 표시

### 4. 실험 자동화 추가

- 서울 O/D 샘플 세트 추가
- 로컬 API를 batch 호출하는 실험 스크립트 추가
- baseline / recommended / best mixed alternative 비교 결과를 JSON/Markdown으로 저장
- 현재 샘플 기준 추천은 모두 `TRANSIT_ONLY`이며,
  best mixed alternative는 평균적으로 시간 +11.625분, 도보 +592.625m, 비용 +806.25원 불리함

관련 파일:

- `docs/experiments/od-samples.seoul.json`
- `docs/experiments/README.md`
- `scripts/evaluate_routes.py`
- `output/experiments/latest-route-eval.json`
- `output/experiments/latest-route-eval.md`

관련 파일:

- `frontend/src/components/route/RouteCard.jsx`
- `frontend/src/utils/routeInsights.js`

### 5. 외부 API 호출 최적화

- `ODsay` 경로 조회에 30초 TTL 캐시 추가
- `getTransitTimeMinutes()`가 같은 출발/도착 쌍에서 경로 캐시를 재사용하도록 정리
- `따릉이`는 전체 정류소 snapshot을 30초 캐시하고, 반경 필터링만 재계산하도록 변경
- `킥보드(TAGO)`도 전체 기기 snapshot을 30초 캐시하고, 반경 필터링만 재계산하도록 변경
- `MobilityAvailabilityAdapter`에 20초 TTL 캐시를 추가해 같은 환승점의 pickup/dropoff 조회를 재사용
- `TMAP` 보행자 경로 조회는 5분 TTL 캐시 추가
- `SpecificMobilityStrategy`의 라스트마일 허브 후보를 최대 3개로 제한
- 모든 캐시에 `navigation.cache.total` hit/miss metric 추가
- 캐시 TTL을 `application.yml` 설정으로 분리
- batch 실험 스크립트가 actuator metric을 읽어 cache hit/miss delta를 함께 기록하도록 변경
- 라스트마일 허브는 목적지 기준 이동수단 최대 범위 이내 후보만 남기도록 pruning 추가
- 매우 가까운 후보 정류소는 하나로 dedup 처리
- 무료 요금제 기준으로 동일 탐색 내 중복 호출을 줄이는 방향으로 먼저 최적화

관련 파일:

- `infra/odsay/OdsayRouteClient`
- `infra/ddareungi/DdareungiApiClient`
- `infra/kickboard/KickboardApiClient`
- `infra/adapter/MobilityAvailabilityAdapter`
- `infra/tmap/TmapPedestrianClient`
- `application/route/strategy/SpecificMobilityStrategy`
- `application/route/HubSelector`
- `application/route/CandidatePointSelector`
- `scripts/evaluate_routes.py`

## 검증

- `./gradlew :domain:test :application:test :api:test :infra:test --no-daemon` 통과
- `frontend` `npm run build` 통과
- 외부 API 캐시 적용 후에도 전체 테스트/빌드 통과

## 남은 과제

### 1. Hub 의미 고도화

- 현재는 actual route leg 기반 허브 추출
- 다음 단계는 선택된 candidate hub metadata와 실제 route hub를 연결해 보여주기

### 2. 평가 지표 실험화

- `RouteEvaluation`을 기반으로 batch 실험/비교 지표 생성
- baseline 대비 시간/도보/비용/신뢰도 비교 자동화

### 3. README 반영

- `RouteEvaluation`, `RouteHub`, 비용 정책 가정을 README에 추가

### 4. 호출 수 추가 절감

- 허브 후보 pruning 강화
- 부분 대중교통 구간 route/time 중복 계산 구조 축소
- 실험 스크립트에 API 호출량/캐시 hit 추적 추가

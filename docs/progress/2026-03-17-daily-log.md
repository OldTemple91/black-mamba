# 2026-03-17 Daily Log

## 오늘 한 일

- `no-mixed` 병목이 `NO_PICKUP` 중심으로 모이는지 다시 확인
- pickup/dropoff 반경 분리(`pickup 850m / dropoff 700m`)를 실험했지만, mixed-winning 성능 악화로 원복
- selected candidate 허브에 `selectionStrategy(PRIMARY / FALLBACK_NEAREST)`를 UI에 노출
- 진단 메시지에 후보 구성(`기본 n개, fallback n개`)이 반영되도록 보강
- 최신 코드 기준 검증 서버를 `8082`로 다시 올려 실 API 응답 확인

## 핵심 결과

### 반경 분리 실험

- 시도:
  - pickup `850m`
  - dropoff `700m`
- 결과:
  - `no-mixed` 세트는 개선되지 않음
  - `mixed-winning` 7건이 `7/7 winner`에서 `3/7 winner` 수준으로 악화
- 판단:
  - 현재 엔진에서는 반경 확대보다 허브 선택 규칙 보강이 더 효과적
  - 반경 분리 변경은 커밋하지 않고 원복

### 허브/진단 설명력

- selected candidate 허브에 `기본 후보` / `근접 fallback` 표시 추가
- `NO_PICKUP` 진단은 이제 fallback이 섞인 경우 후보 구성을 함께 보여줄 수 있음
- `NO_PICKUP` / `NO_DROPOFF` 진단은 최근접 대여/반납 정류소 힌트를 붙일 수 있게 보강

## 검증

```bash
./gradlew :application:test :api:test --no-daemon
cd frontend && npm run build
```

- 결과: 모두 성공

## 현재 판단

- `NO_PICKUP`는 단순 반경 확장으로 해결할 문제가 아님
- 허브 fallback은 유효했고, 이제는 “왜 이 fallback 허브도 실패했는지”를 더 잘 보여주는 쪽이 맞음
- 다음은 캠퍼스/박물관/업무지구 내부처럼 허브 선택이 약한 목적지 유형을 더 세밀하게 다루는 작업이 우선

## 다음 우선순위

1. `NO_PICKUP` 진단에 최근접 정류소/대여 가능성 힌트 추가 검토
2. 캠퍼스/박물관/업무지구 내부용 허브 선택 규칙 보강
3. 허브 선택 전략 표시를 README/실험 문서에도 반영

## 추가 업데이트

- 최근접 정류소 힌트 기능을 실제 코드에 반영
  - `MobilityAvailabilityPort.findNearestMobilityHint(...)` 추가
  - `MobilityAvailabilityAdapter`가 따릉이 snapshot을 재사용해 최근접 정류소 힌트 제공
  - `GenerationDiagnostic` 메시지에서 `NO_PICKUP`/`NO_DROPOFF`에 따라 대여/반납 후보 힌트를 분리
- 최신 8082 서버 재검증 결과:
  - 힌트가 없으면 메시지를 깔끔하게 생략
  - 힌트가 있는 경우에만 자연어 suffix로 붙도록 동작
